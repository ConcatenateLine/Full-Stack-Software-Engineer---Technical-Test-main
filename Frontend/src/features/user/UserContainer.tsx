import { Button } from "@/components/ui/button";

const UserContainer = () => {
  return (
    <div>
      UserContainer
      <Button onClick={() => console.log("Click Event")}>cast</Button>
    </div>
  );
};

export default UserContainer;

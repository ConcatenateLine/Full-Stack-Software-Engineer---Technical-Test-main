export class Address {
  constructor(
    public street: string,
    public number: string,
    public city: string,
    public postalCode: string,
    public lat: string,
    public lng: string
  ) {}
}
